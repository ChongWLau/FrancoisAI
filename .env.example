# Supabase — copy from Project Settings > API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Gemini — add when setting up the AI agent middleware (server-side only, no VITE_ prefix)
# GEMINI_API_KEY=your-gemini-api-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key  (for agent middleware to bypass RLS)
